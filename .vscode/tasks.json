{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "python",
			"dockerBuild": {
				"tag": "dementia2:latest",
				"dockerfile": "${workspaceFolder}/Dockerfile.dev",
				"context": "${workspaceFolder}",
				"pull": true
			}
		}
	]
}